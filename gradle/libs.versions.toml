[versions]
surf-database = "1.0.5-SNAPSHOT"
luckperms-api = "5.4"
packetevents-velocity = "2.8.0"

[libraries]
surf-database = { module = "dev.slne.surf:surf-database", version.ref = "surf-database" }
luckperms-api = { module = "net.luckperms:api", version.ref = "luckperms-api" }
packetevents-velocity = { module = "com.github.retrooper:packetevents-velocity", version.ref = "packetevents-velocity" }